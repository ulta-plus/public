# GCP configuration
PROJECT_ID=your-project-id
GCP_SA_KEY=your-service-account-key

# Yandex Cloud configuration

YANDEX_ACCESS_KEY_ID=
YANDEX_SECRET_ACCESS_KEY=
YANDEX_BUCKET=

# Deployment configuration
# Comma-separated list of directories that can be deployed to GCP buckets
# Each directory should have a corresponding GCP bucket with the same name
# Configure this in GitHub Actions Variables as ALLOWED_DIRS
ALLOWED_DIRS=uboost,uboost-neo,vpn-web,naruzhu

# Set DEPLOY_TYPE to 'all' to deploy all files from allowed directories, 
# otherwise only changed files will be deployed
DEPLOY_TYPE=
