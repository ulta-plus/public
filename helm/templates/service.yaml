apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.projectName }}-app
  labels:
    environment: {{ .Values.environment }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.projectName }}-app
  ports:
    - name: web
      port: 8080
      targetPort: web
